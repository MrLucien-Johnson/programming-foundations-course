/**
 * Local/dev API endpoint. Override for production deploys.
 * This file can be committed with localhost for demos, or replaced in CI.
 */
window.PF_CONFIG = {
  apiBaseUrl: "http://localhost:8787",
};
