# Quiz — Compute Engine

1. What makes VM creation repeatable for a managed instance group?
   - A. Instance template
   - B. Billing export
   - C. DNS policy
   - D. IAM condition only

2. What does a managed instance group health check support?
   - A. Autohealing unhealthy instances
   - B. Creating projects
   - C. Encrypting IAM
   - D. Changing invoices

3. Which workloads fit Spot VMs?
   - A. Interruption-tolerant batch jobs
   - B. A single critical database with no replica
   - C. Uncheckpointed long transactions
   - D. Hardware license servers only

4. Why use `--no-address`?
   - A. To avoid an external VM IP
   - B. To remove the internal IP
   - C. To disable all routes
   - D. To create a load balancer

5. What should the attached service account have?
   - A. Only required roles
   - B. Project Owner
   - C. No identity ever
   - D. A downloaded key on disk
