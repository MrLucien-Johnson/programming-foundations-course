# Quiz — State & backends

1. What does state map?
   - A. Terraform addresses to remote objects
   - B. Users to passwords
   - C. Logs to traces
   - D. Regions to invoices

2. Why use backend locking?
   - A. To prevent concurrent state writes
   - B. To freeze cloud APIs
   - C. To encrypt Git
   - D. To stop all plans

3. Can sensitive values still exist in state?
   - A. Yes
   - B. No
   - C. Only in comments
   - D. Only after destroy

4. What supports an address-only refactor?
   - A. A `moved` block
   - B. Deleting state
   - C. Changing provider credentials
   - D. Skipping plan

5. What does `terraform state rm` do?
   - A. Stops Terraform managing the object without deleting it
   - B. Deletes the remote object
   - C. Locks the backend
   - D. Formats configuration
