# Quiz — Modules

1. What should a child module represent?
   - A. A cohesive infrastructure capability
   - B. Every company resource
   - C. Only one variable file
   - D. A backend lock

2. Why pin a remote module version?
   - A. To make consumed code reproducible
   - B. To disable outputs
   - C. To avoid state
   - D. To create credentials

3. What prevents recreation during module refactoring?
   - A. State address migration with `moved`
   - B. Renaming files only
   - C. Skipping refresh
   - D. Deleting the backend

4. What belongs in a module interface?
   - A. Typed inputs and useful outputs
   - B. Provider credentials
   - C. Local state
   - D. Every possible knob

5. What command formats nested modules?
   - A. terraform fmt -recursive
   - B. terraform state push
   - C. terraform force-unlock
   - D. terraform output -raw
