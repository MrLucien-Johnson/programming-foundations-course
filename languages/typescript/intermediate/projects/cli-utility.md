# CLI Utility Project

Build a TypeScript command-line utility that solves a real task.

## Requirements
- argument parsing and validation
- reusable modules with clear types
- automated tests or scripted validation
- README with setup and usage examples
