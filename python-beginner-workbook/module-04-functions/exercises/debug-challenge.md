# Debug Challenge: Find the Errors

Find and fix errors in this broken code:

```python
def calculate_total(price tax_rate):
    total = price + (price * tax_rate)
    return

result = calculate_total(100, 0.20)
print(result)
```

**Errors to find:**
1. Missing comma in function definition
2. Missing return value
3. Other issues?

See `debug-challenge-solution.md` for solution.

